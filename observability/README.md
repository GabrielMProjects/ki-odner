# observability

Monitoring, Logging und Metriken für die Anwendung.

**Zweck:** Einblick in den Betrieb – z. B. Prometheus (Metriken) + Grafana (Dashboards),
plus zentrale Logs. Lokal testbar via Docker Compose.

> Konfiguration (Compose-Stack, Dashboards, Alerts) wird in einem späteren Abschnitt befüllt.

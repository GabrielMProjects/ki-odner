apiVersion: v1
kind: Service
metadata:
  name: {{ include "angel-lara.frontend.fullname" . }}
  labels:
    {{- include "angel-lara.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "angel-lara.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
